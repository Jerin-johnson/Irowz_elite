<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Successful | Irowz Elite</title>
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn-uicons.flaticon.com/uicons-regular-straight/css/uicons-regular-straight.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <%- include('../partials/userprofile/style') %>
    <style>
        .success-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px 20px;
            text-align: center;
        }
        
        .success-animation {
            margin-bottom: 30px;
            position: relative;
        }
        
        .success-icon {
            width: 120px;
            height: 120px;
            background: linear-gradient(135deg, #28a745, #20c997);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            animation: successPulse 2s ease-in-out infinite;
            box-shadow: 0 10px 30px rgba(40, 167, 69, 0.3);
        }
        
        .success-icon i {
            font-size: 48px;
            color: white;
            animation: checkmark 0.8s ease-in-out;
        }
        
        @keyframes successPulse {
            0%, 100% {
                transform: scale(1);
                box-shadow: 0 10px 30px rgba(40, 167, 69, 0.3);
            }
            50% {
                transform: scale(1.05);
                box-shadow: 0 15px 40px rgba(40, 167, 69, 0.4);
            }
        }
        
        @keyframes checkmark {
            0% {
                transform: scale(0);
                opacity: 0;
            }
            50% {
                transform: scale(1.2);
            }
            100% {
                transform: scale(1);
                opacity: 1;
            }
        }
        
        .success-title {
            font-size: 2.5rem;
            font-weight: 800;
            color: #333;
            margin-bottom: 15px;
            animation: slideInUp 0.8s ease-out 0.3s both;
        }
        
        .success-subtitle {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 30px;
            line-height: 1.6;
            animation: slideInUp 0.8s ease-out 0.5s both;
        }
        
        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .order-summary-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            padding: 30px;
            margin: 40px 0;
            text-align: left;
            animation: slideInUp 0.8s ease-out 0.7s both;
        }
        
        .order-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f8f9fa;
        }
        
        .order-id {
            font-size: 1.1rem;
            font-weight: 700;
            color: #333;
        }
        
        .order-date {
            color: #666;
            font-size: 0.95rem;
        }
        
        .order-status {
            background: linear-gradient(135deg, #ffc107, #fd7e14);
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .order-items {
            margin-bottom: 25px;
        }
        
        .order-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 15px 0;
            border-bottom: 1px solid #f0f0f0;
        }
        
        .order-item:last-child {
            border-bottom: none;
        }
        
        .item-image {
            width: 60px;
            height: 60px;
            border-radius: 8px;
            overflow: hidden;
            flex-shrink: 0;
        }
        
        .item-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .item-details {
            flex: 1;
        }
        
        .item-name {
            font-weight: 600;
            color: #333;
            margin-bottom: 5px;
            font-size: 0.95rem;
        }
        
        .item-quantity {
            color: #666;
            font-size: 0.85rem;
        }
        
        .item-price {
            color: #b4883e;
            font-weight: 700;
            font-size: 1rem;
        }
        
        .order-total {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-top: 20px;
        }
        
        .total-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            color: #666;
        }
        
        .total-row.final {
            font-size: 1.2rem;
            font-weight: 700;
            color: #333;
            border-top: 2px solid #e9ecef;
            padding-top: 15px;
            margin-top: 15px;
            margin-bottom: 0;
        }
        
        .action-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-top: 40px;
            animation: slideInUp 0.8s ease-out 0.9s both;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #b4883e, #8e6b2f);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 4px 15px rgba(180, 136, 62, 0.3);
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(180, 136, 62, 0.4);
            text-decoration: none;
            color: white;
        }
        
        .btn-outline {
            background: transparent;
            color: #b4883e;
            border: 2px solid #b4883e;
            padding: 13px 28px;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }
        
        .btn-outline:hover {
            background: #b4883e;
            color: white;
            transform: translateY(-2px);
            text-decoration: none;
        }
        
        .delivery-info {
            background: linear-gradient(135deg, #e3f2fd, #f3e5f5);
            border-radius: 12px;
            padding: 25px;
            margin: 30px 0;
            animation: slideInUp 0.8s ease-out 1.1s both;
        }
        
        .delivery-title {
            font-size: 1.1rem;
            font-weight: 700;
            color: #333;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .delivery-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
        }
        
        .delivery-item {
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .delivery-icon {
            width: 40px;
            height: 40px;
            background: rgba(180, 136, 62, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #b4883e;
        }
        
        .delivery-text {
            flex: 1;
        }
        
        .delivery-label {
            font-size: 0.85rem;
            color: #666;
            margin-bottom: 2px;
        }
        
        .delivery-value {
            font-weight: 600;
            color: #333;
        }
        
        .confetti {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1000;
        }
        
        .confetti-piece {
            position: absolute;
            width: 10px;
            height: 10px;
            background: #b4883e;
            animation: confetti-fall 3s linear infinite;
        }
        
        @keyframes confetti-fall {
            0% {
                transform: translateY(-100vh) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(100vh) rotate(720deg);
                opacity: 0;
            }
        }
        
        /* Mobile Responsive */
        @media (max-width: 768px) {
            .success-container {
                padding: 20px 15px;
            }
            
            .success-title {
                font-size: 2rem;
            }
            
            .success-subtitle {
                font-size: 1rem;
            }
            
            .order-summary-card {
                padding: 20px;
                margin: 30px 0;
            }
            
            .order-header {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }
            
            .action-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .btn-primary, .btn-outline {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }
            
            .delivery-details {
                grid-template-columns: 1fr;
            }
            
            .order-item {
                flex-direction: column;
                text-align: center;
                gap: 10px;
            }
            
            .item-details {
                text-align: center;
            }
        }
        
        @media (max-width: 480px) {
            .success-icon {
                width: 100px;
                height: 100px;
            }
            
            .success-icon i {
                font-size: 40px;
            }
            
            .success-title {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/userprofile/header') %>
    
    <!-- Confetti Animation -->
    <div class="confetti" id="confetti"></div>
    
    <div class="container">
        <div class="success-container">
            <!-- Success Animation -->
            <div class="success-animation">
                <div class="success-icon">
                    <i class="fas fa-check"></i>
                </div>
            </div>
            
            <!-- Success Message -->
            <h1 class="success-title">Order Placed Successfully!</h1>
            <p class="success-subtitle">
                Thank you for your purchase! Your order has been confirmed and is being processed. 
                We'll send you updates about your order status via email and SMS.
            </p>
            
            <!-- Order Summary Card -->
            <div class="order-summary-card">
                <div class="order-header">
                    <div>
                        <div class="order-id">Order ID: #<%= order.orderId %></div>
                        <div class="order-date">
                            Placed on <%= new Date(order.createdOn).toLocaleDateString('en-US', { 
                                weekday: 'long', 
                                year: 'numeric', 
                                month: 'long', 
                                day: 'numeric',
                                hour: '2-digit',
                                minute: '2-digit'
                            }) %>
                        </div>
                    </div>
                    <div class="order-status"><%= order.status %></div>
                </div>
                
                <!-- Order Items -->
                <div class="order-items">
                    <h3 style="margin-bottom: 20px; color: #333;">
                        <i class="fas fa-shopping-bag" style="color: #b4883e; margin-right: 10px;"></i>
                        Order Items (<%= order.items.length %> items)
                    </h3>
                    
                    <% order.items.forEach((item) => { %>
                    <div class="order-item">
                        <div class="item-image">
                            <img src="/<%=item.productImage %>" 
                                 alt="<%= item.productName %>">
                        </div>
                        <div class="item-details">
                            <div class="item-name"><%= item.productId.productName %></div>
                            <div class="item-quantity">Quantity: <%= item.quantity %></div>
                        </div>
                        <div class="item-price">$<%= (item.price * item.quantity).toFixed(2) %></div>
                    </div>
                    <% }); %>
                </div>
                
                <!-- Order Total -->
                <div class="order-total">
                    <div class="total-row">
                        <span>Subtotal:</span>
                        <span>$<%= order.totalAmount.toFixed(2) %></span>
                    </div>
                    <% if (order.discount > 0) { %>
                    <div class="total-row">
                        <span>Discount:</span>
                        <span style="color: #28a745;">-$<%= order.discount.toFixed(2) %></span>
                    </div>
                    <% } %>
                    <div class="total-row">
                        <span>Tax:</span>
                        <span>$<%= order.tax.toFixed(2) %></span>
                    </div>
                    <div class="total-row">
                        <span>Shipping:</span>
                        <span style="color: <%= order.totalPrice > 75 ? '#28a745' : '#333' %>;">
                            <%= order.totalPrice > 75? 'Free' : '$' + ((order.finalAmount - order.totalAmount + order.discount) * 0.07).toFixed(2) %>
                        </span>
                    </div>
                    <div class="total-row final">
                        <span>Total Amount:</span>
                        <span>$<%= order.finalAmount.toFixed(2) %></span>
                    </div>
                </div>
            </div>
            
            <!-- Delivery Information -->
            <div class="delivery-info">
                <div class="delivery-title">
                    <i class="fas fa-truck"></i>
                    Delivery Information
                </div>
                <div class="delivery-details">
                    <div class="delivery-item">
                        <div class="delivery-icon">
                            <i class="fas fa-calendar-alt"></i>
                        </div>
                        <div class="delivery-text">
                            <div class="delivery-label">Expected Delivery</div>
                            <div class="delivery-value">
                                <%= new Date(Date.now() + 5 * 24 * 60 * 60 * 1000).toLocaleDateString('en-US', { 
                                    weekday: 'long', 
                                    month: 'long', 
                                    day: 'numeric' 
                                }) %>
                            </div>
                        </div>
                    </div>
                    
                    <div class="delivery-item">
                        <div class="delivery-icon">
                            <i class="fas fa-shipping-fast"></i>
                        </div>
                        <div class="delivery-text">
                            <div class="delivery-label">Shipping Method</div>
                            <div class="delivery-value">Standard Delivery</div>
                        </div>
                    </div>
                    
                    <div class="delivery-item">
                        <div class="delivery-icon">
                            <i class="fas fa-bell"></i>
                        </div>
                        <div class="delivery-text">
                            <div class="delivery-label">Tracking Updates</div>
                            <div class="delivery-value">Via Email & SMS</div>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Action Buttons -->
            <div class="action-buttons">
                <a href="/orders/<%= order.orderId %>" class="btn-primary">
                    <i class="fas fa-receipt"></i>
                    View Order Details
                </a>
                <a href="/shop" class="btn-outline">
                    <i class="fas fa-shopping-cart"></i>
                    Continue Shopping
                </a>
            </div>
            
            <!-- Additional Information -->
            <div style="margin-top: 50px; padding: 30px; background: #f8f9fa; border-radius: 12px; animation: slideInUp 0.8s ease-out 1.3s both;">
                <h3 style="color: #333; margin-bottom: 20px; text-align: center;">
                    <i class="fas fa-info-circle" style="color: #b4883e; margin-right: 10px;"></i>
                    What's Next?
                </h3>
                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; text-align: left;">
                    <div style="display: flex; align-items: flex-start; gap: 15px;">
                        <div style="width: 40px; height: 40px; background: rgba(40, 167, 69, 0.1); border-radius: 50%; display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
                            <i class="fas fa-envelope" style="color: #28a745;"></i>
                        </div>
                        <div>
                            <h4 style="margin: 0 0 8px; color: #333;">Order Confirmation</h4>
                            <p style="margin: 0; color: #666; font-size: 0.9rem;">You'll receive an email confirmation with your order details shortly.</p>
                        </div>
                    </div>
                    
                    <div style="display: flex; align-items: flex-start; gap: 15px;">
                        <div style="width: 40px; height: 40px; background: rgba(255, 193, 7, 0.1); border-radius: 50%; display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
                            <i class="fas fa-cogs" style="color: #ffc107;"></i>
                        </div>
                        <div>
                            <h4 style="margin: 0 0 8px; color: #333;">Order Processing</h4>
                            <p style="margin: 0; color: #666; font-size: 0.9rem;">We'll prepare your items and get them ready for shipment.</p>
                        </div>
                    </div>
                    
                    <div style="display: flex; align-items: flex-start; gap: 15px;">
                        <div style="width: 40px; height: 40px; background: rgba(180, 136, 62, 0.1); border-radius: 50%; display: flex; align-items: center; justify-content: center; flex-shrink: 0;">
                            <i class="fas fa-truck" style="color: #b4883e;"></i>
                        </div>
                        <div>
                            <h4 style="margin: 0 0 8px; color: #333;">Shipment & Delivery</h4>
                            <p style="margin: 0; color: #666; font-size: 0.9rem;">Track your package and receive it at your doorstep.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <%- include('../partials/userprofile/footer') %>
    
    <script>
        // Confetti Animation
        function createConfetti() {
            const confettiContainer = document.getElementById('confetti');
            const colors = ['#b4883e', '#28a745', '#ffc107', '#dc3545', '#6f42c1', '#20c997'];
            
            for (let i = 0; i < 50; i++) {
                const confettiPiece = document.createElement('div');
                confettiPiece.className = 'confetti-piece';
                confettiPiece.style.left = Math.random() * 100 + '%';
                confettiPiece.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                confettiPiece.style.animationDelay = Math.random() * 3 + 's';
                confettiPiece.style.animationDuration = (Math.random() * 2 + 2) + 's';
                confettiContainer.appendChild(confettiPiece);
            }
            
            // Remove confetti after animation
            setTimeout(() => {
                confettiContainer.innerHTML = '';
            }, 5000);
        }
        
        // Start confetti animation when page loads
        window.addEventListener('load', () => {
            setTimeout(createConfetti, 500);
        });
        
        // Add click tracking for buttons
        document.querySelectorAll('.btn-primary, .btn-outline').forEach(button => {
            button.addEventListener('click', function(e) {
                // Add click animation
                this.style.transform = 'scale(0.95)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 150);
            });
        });
        
        // Auto-scroll to center the success message
        window.addEventListener('load', () => {
            setTimeout(() => {
                document.querySelector('.success-container').scrollIntoView({
                    behavior: 'smooth',
                    block: 'center'
                });
            }, 100);
        });
    </script>
</body>
</html>
