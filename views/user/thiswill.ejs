<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>IrowzElite - Luxury Watches</title>


    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">

    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <link rel="stylesheet" href="/css/home.css" /> 
  </head>
  <body>


    <%- include('../partials/user/header') %>

    <section class="carousel">
      <div class="carousel-slides">
        <div class="carousel-slide active">
          <img src="https://via.placeholder.com/1200x600?text=Summer+Collection+2025" alt="Summer Collection 2025" />
          <div class="carousel-content">
            <span class="collection-label">Summer Collection</span>
            <h1>Hots - Summer Collections 2025</h1>
            <p>A specialist label creating luxury essentials.</p>
            <a href="/products" class="shop-now-btn">Shop Now</a>
          </div>
        </div>
        <div class="carousel-slide">
          <img src="https://via.placeholder.com/1200x600?text=Winter+Collection+2025" alt="Winter Collection 2025" />
          <div class="carousel-content">
            <span class="collection-label">Winter Collection</span>
            <h1>Hots - Winter Collections 2025</h1>
            <p>Discover the latest in luxury watches.</p>
            <a href="/products" class="shop-now-btn">Shop Now</a>
          </div>
        </div>
        <div class="carousel-slide">
          <img src="https://via.placeholder.com/1200x600?text=Spring+Collection+2025" alt="Spring Collection 2025" />
          <div class="carousel-content">
            <span class="collection-label">Spring Collection</span>
            <h1>Hots - Spring Collections 2025</h1>
            <p>Elegance in every detail.</p>
            <a href="/products" class="shop-now-btn">Shop Now</a>
          </div>
        </div>
      </div>
      <div class="carousel-dots">
        <span class="dot active" data-slide="0"></span>
        <span class="dot" data-slide="1"></span>
        <span class="dot" data-slide="2"></span>
      </div>
    </section>





<section class="product-section new-arrivals">
  <h2>New Arrivals</h2>
  <div class="product-container d-flex flex-wrap justify-content-start">
    <% if (products && products.length > 0) { %>
      <% products.forEach((product) => { %>
        <a href="/productdetail/?id=<%= product._id %>" class="product-card text-decoration-none">
          <img src="<%= product.productImage[0]?.path || '/images/fallback.jpg' %>" alt="<%= product.productName %>" />
          <h3 class="text-dark"><%= product.productName %></h3>
          <div class="price">
            <span class="original-price text-muted text-decoration-line-through">₹<%= product.regularPrice %></span>
            <span class="discounted-price text-danger fw-bold ms-2">₹<%= product.salePrice %></span>
          </div>
          <span class="discount badge bg-success"><%= product.discount %>% OFF</span>
          <span class="stock-status badge <%= product.status === 'In Stock' ? 'bg-primary' : 'bg-secondary' %>"><%= product.status %></span>
          <div class="rating">
            <% for (let i = 0; i < 5; i++) { %>
              <span class="star <%= i < product.rating ? 'filled text-warning' : 'text-muted' %>">★</span>
            <% } %>
            <span>(<%= product.reviews || 0 %>)</span>
          </div>
        </a>
      <% }) %>
    <% } else { %>
      <p>No products found.</p>
    <% } %>
  </div>
</section>



    <%- include('../partials/user/footer') %>


    <!-- <script>
  document.addEventListener("DOMContentLoaded", function () {
    const slides = document.querySelectorAll(".carousel-slide");
    const dots = document.querySelectorAll(".carousel-dots .dot");
    let currentSlide = 0;

    function showSlide(index) {
      slides.forEach((slide, i) => {
        slide.classList.toggle("active", i === index);
      });
      dots.forEach((dot, i) => {
        dot.classList.toggle("active", i === index);
      });
    }

    dots.forEach((dot, index) => {
      dot.addEventListener("click", () => {
        currentSlide = index;
        showSlide(currentSlide);
      });
    });

    setInterval(() => {
      currentSlide = (currentSlide + 1) % slides.length;
      showSlide(currentSlide);
    }, 5000);

   
</script>

  </body>
</html>  -->